This is a toy basic http health check project dabbling in async.

Uses a yaml config as input, iterates over the requested urls and displays the results. 

If using vscode build the provided .devcontainer.

Example usage: uv run httpmon/src/main.py --config httpmon/targets.yaml.
